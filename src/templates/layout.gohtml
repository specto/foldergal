{{ define "layout" }}
<!DOCTYPE html>
<html lang="en">
<head>
    {{ if .Title }}
    <title>{{.Title}}</title>
    {{ end }}
    {{template "head" .}}
</head>
<body>
    {{template "body" .}}
    {{template "footer" .}}
</body>
</html>
{{end}}